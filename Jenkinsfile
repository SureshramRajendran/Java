pipeline {
  agent any
  stages {
    stage('Pre-build') {
      steps {
        echo 'Pre-Build'
      }
    }

    stage('Built') {
      steps {
        echo 'built'
      }
    }

    stage('deploy') {
      steps {
        echo 'deploy'
      }
    }

  }
}